# shared-worker

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-worker` to build the library.

## Running unit tests

Run `nx test shared-worker` to execute the unit tests via [Jest](https://jestjs.io).
