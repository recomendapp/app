# shared-mailer

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-mailer` to build the library.

## Running unit tests

Run `nx test shared-mailer` to execute the unit tests via [Jest](https://jestjs.io).
